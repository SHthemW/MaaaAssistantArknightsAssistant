<Window x:Class="Game_Daily_Routine_Launcher.WebhookRelayHelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Webhook 中转帮助"
        Width="680"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FAFAFA">
    <Window.Resources>
        <Style x:Key="RelayPreviewTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#F3F4F6" />
            <Setter Property="BorderBrush" Value="#D0D0D0" />
            <Setter Property="Foreground" Value="#555555" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Webhook 中转只需要替换企业微信机器人的请求 URL，Body 保持原样。"
                   TextWrapping="Wrap"
                   Foreground="#212121" />

        <TextBlock Grid.Row="1" Text="原 URL 示例" Margin="0,16,0,4" FontWeight="SemiBold" />
        <TextBox Grid.Row="2"
                 Text="{Binding SourceUrlExample, Mode=OneWay}"
                 Style="{StaticResource RelayPreviewTextBoxStyle}" />

        <TextBlock Grid.Row="3" Text="替换为" Margin="0,12,0,4" FontWeight="SemiBold" />
        <Grid Grid.Row="4">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0"
                     Text="{Binding ForwardUrlExample, Mode=OneWay}"
                     Style="{StaticResource RelayPreviewTextBoxStyle}"
                     Margin="0,0,8,0" />
            <Button Grid.Column="1"
                    Content="复制"
                    Style="{StaticResource SmallButton}"
                    MinWidth="64"
                    Click="OnCopyForwardUrlClicked" />
        </Grid>

        <TextBlock Grid.Row="5"
                   Margin="0,12,0,0"
                   Foreground="#616161"
                   Text="程序会监听当前端口，收到该路径后自动转发到真实企业微信地址，并记录实际 URL 与 Body。"
                   TextWrapping="Wrap" />
    </Grid>
</Window>
