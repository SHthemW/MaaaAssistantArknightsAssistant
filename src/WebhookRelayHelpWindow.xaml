<Window x:Class="Game_Daily_Routine_Launcher.WebhookRelayHelpWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Webhook 中转帮助"
        Width="720"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        Background="#FAFAFA">
    <Window.Resources>
        <Style x:Key="RelayPreviewTextBoxStyle" TargetType="TextBox">
            <Setter Property="Background" Value="#F3F4F6" />
            <Setter Property="BorderBrush" Value="#D0D0D0" />
            <Setter Property="Foreground" Value="#555555" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="VerticalContentAlignment" Value="Center" />
        </Style>
    </Window.Resources>
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Text="Webhook 中转会在本地端口接收请求，并将请求转发到你配置的目标地址。下方输入原始推送内容，程序会实时生成中转时应替换的 url 和 body。" TextWrapping="Wrap" Foreground="#212121" />

        <TextBlock Grid.Row="1" Text="原始推送 URL" Margin="0,16,0,4" FontWeight="SemiBold" />
        <TextBox Grid.Row="2" Text="{Binding SourceUrl, UpdateSourceTrigger=PropertyChanged}" Background="White" />

        <TextBlock Grid.Row="3" Text="原始推送 Body" Margin="0,12,0,4" FontWeight="SemiBold" />
        <TextBox Grid.Row="4"
                 Text="{Binding SourceBody, UpdateSourceTrigger=PropertyChanged}"
                 Background="White"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 Height="72"
                 VerticalScrollBarVisibility="Auto" />

        <TextBlock Grid.Row="5" Text="应替换为的转发 URL" Margin="0,12,0,4" FontWeight="SemiBold" />
        <Grid Grid.Row="6">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0"
                     Text="{Binding ForwardUrl, Mode=OneWay}"
                     Style="{StaticResource RelayPreviewTextBoxStyle}"
                     Margin="0,0,8,0" />
            <Button Grid.Column="1"
                    Content="复制"
                    Style="{StaticResource SmallButton}"
                    MinWidth="64"
                    Click="OnCopyForwardUrlClicked" />
        </Grid>

        <TextBlock Grid.Row="7" Text="应替换为的转发 Body" Margin="0,12,0,4" FontWeight="SemiBold" />
        <Grid Grid.Row="8">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <TextBox Grid.Column="0"
                     Text="{Binding ForwardBody, Mode=OneWay}"
                     Style="{StaticResource RelayPreviewTextBoxStyle}"
                     AcceptsReturn="True"
                     TextWrapping="Wrap"
                     VerticalScrollBarVisibility="Auto"
                     MinHeight="96"
                     Margin="0,0,8,0" />
            <Button Grid.Column="1"
                    Content="复制"
                    Style="{StaticResource SmallButton}"
                    MinWidth="64"
                    VerticalAlignment="Top"
                    Click="OnCopyForwardBodyClicked" />
        </Grid>
    </Grid>
</Window>
