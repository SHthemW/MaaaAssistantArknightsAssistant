# CHANGELOG

本文档只记录当前分支相对 `origin/master` 的最终差异，不包含分支开发过程中已在分支内自行发现并解决、且最终未体现在交付结果中的中间问题。

对比范围：

- 基线：`origin/master` (`ff23869`)
- 当前：`dev_260617` (`c91737c`)

## 新增

### 桌面应用与统一配置

- 新增基于 WPF 的桌面应用，用图形界面统一管理任务链、日志、通知和自动运行配置。
- 新增 `appsettings.Local.json` 本地配置机制，支持界面内实时修改并自动保存。
- 新增任务卡片式配置界面，可直接编辑工具路径、参数、监控进程、启动延迟、超时时间和启用状态。
- 新增旧版批处理任务自动导入能力，可从 `launch.bat`、`run_*.bat` 和 `monitor_*.bat` 中迁移启动路径、参数与监控进程到新版任务列表。
- 新增单任务启动与整链启动两种执行方式。

### 任务调度与执行

- 新增多任务链顺序编排能力，前一项任务结束后自动进入下一项。
- 新增基于进程轮询的任务监控与超时控制。
- 新增自动运行时间窗控制，支持跨天时间段。
- 新增时间窗内随机启动能力。
- 新增自动运行模式下不在时间窗内时静默退出的行为。

### 防打扰与收尾能力

- 新增启动时自动静音能力。
- 新增运行结束后恢复原始静音状态和音量的能力。
- 新增基于 Twinkle Tray 的亮度压低与恢复能力。
- 新增静音与亮度控制的手动测试入口。
- 新增任务完成后自动关机能力，并支持限制为仅自动运行时生效。

### 通知与日志

- 新增界面运行日志面板，支持自动滚动、清空和导出。
- 新增运行日志落盘能力，日志写入 `logs/session-*.log`。
- 新增 Webhook 推送能力，支持自定义请求体模板。
- 新增按日志类别筛选 Webhook 推送内容的能力。
- 新增“仅推送 AI 总结”模式。
- 新增本地 Webhook 中转服务与帮助窗口，可将本地接收地址按原始 URL 路径转发到真实目标地址。

### AI 智能总结

- 新增智谱 AI 总结能力，用于基于任务状态和当日运行日志生成结果总结。
- 新增 ChatGPT 与 DeepSeek 总结平台；各平台分别保存 API Key、API URL 和模型，提示词、Temperature、超时、重试及流式返回等通用参数由所有平台共享。
- 修复部分 OpenAI 兼容服务返回完整 JSON、Responses 事件或正文为空时解析失败且重复重试的问题，并补充响应结构诊断。
- 修复将 OpenAI 兼容服务基础地址填写为 API URL 时请求到网站首页的问题，并为 HTML 响应提供明确的地址配置提示。
- 新增 AI 总结参数配置，包括 API 地址、模型、提示词、Temperature、超时、重试、深度思考和流式返回。
- 新增 AI 测试入口，可直接验证接口连通性。
- 新增“使用最近日志测试”能力，可复用最近一次提示日志调试总结效果。
- 新增 AI 提示日志落盘能力，日志写入 `logs/aiprompt-*.log`。
- 新增 AI 总结最终结果的 Webhook 推送能力。

## 变更

### 项目结构

- 项目已从纯批处理脚本集合演进为 `.NET 8 + WPF` 桌面应用。
- 原有批处理脚本已归档到 `src/batch/`，便于与桌面应用共存。
- 源代码已集中到 `src/` 目录下，按 `Models`、`Services`、`ViewModels`、`Converters` 等结构拆分。
- README 已补充完整配置说明，逐项说明任务、防打扰、调度、Webhook 与 AI 总结配置的填写方式和生效行为。

### 开机自启机制

- 开机自启已改为基于 Windows 计划任务，而不是依赖旧的注册表方式。
- 计划任务名称已与当前程序实例路径绑定，避免不同目录下的程序实例共用同一个自启项。
- 启动或取消自启时会自动清理旧版本残留的注册表项和共享计划任务项。

### 打包与发布

- 新增 `.csproj` 与解决方案文件，支持标准 `dotnet build`、`dotnet run`、`dotnet publish` 工作流。
- `dotnet publish` 完成后会自动压缩发布目录，并输出到 `bin/Release-Archives/`。

## 兼容性说明

- 运行环境从批处理脚本直接执行，扩展为需要 Windows 桌面环境与 `.NET 8` 的 WPF 应用。
- 首次生成或加载新版配置时，会尝试从程序目录、`batch/` 或 `src/batch/` 中的旧版批处理脚本导入任务配置；导入只补充空字段，不覆盖已经在新版界面中配置过的内容。
- 亮度控制依赖本机已经安装并运行 Twinkle Tray。
- 原神任务默认可通过 `bettergi://` URI 启动，要求系统已注册对应协议。
